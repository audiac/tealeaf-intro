# ex4.rb

puts "The first set of code outputs 1 and 3. It prints the first item to the
      screen then returns an array of the next item using ary.shift(1), prints
      3, then returns an array of the last item, again using ary.shift(1)."

puts "The second set of code prints the first item (1), removes and returns
      the last item using ary.pop(1), prints the next item (2), and removes
      and returns the 3 using ary.pop(1)."